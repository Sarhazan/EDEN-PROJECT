# Production environment variables
# In production, the API is served from the same origin (relative URLs)
VITE_API_URL=/api
# VITE_BACKEND_URL is intentionally not set in production
# When undefined, the code uses empty string for same-origin static asset requests

# Production environment variables
# In production, the API is served from the same origin (relative URLs)
VITE_API_URL=/api
# In production, use empty string for same-origin static asset requests (uploads)
VITE_BACKEND_URL=

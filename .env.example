# Server Configuration
PORT=3002
NODE_ENV=development

# Local development URLs
CLIENT_URL=http://192.168.1.35:5174
API_URL=http://192.168.1.35:3002

# Production public API URL (Render)
# After deploying to Render, update this with your Render URL
# Example: PUBLIC_API_URL=https://eden-maintenance.onrender.com
# This URL is used for:
# 1. WhatsApp confirmation page links (PUBLIC_API_URL/docs/task-xxx.html)
# 2. API calls from confirmation pages
PUBLIC_API_URL=

# URL Shortening (optional - enabled by default)
# Uses is.gd service (https://is.gd) - free, no API key required
# Set to false to disable URL shortening (will send full URLs)
URL_SHORTENER_ENABLED=true

# Google Gemini API (FREE tier - primary translation provider)
# Get key from: https://aistudio.google.com/apikey
# Free tier: 15 requests/minute, 1,500 requests/day
GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud Translation API (PAID fallback - optional)
# Get credentials from: console.cloud.google.com
# Required steps:
# 1. Create Google Cloud project
# 2. Enable Cloud Translation API
# 3. Create service account with 'Cloud Translation API User' role
# 4. Download JSON key file
# 5. Set path to JSON file below
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json
